# BILINGUAL CONTENT RULES

- Stable codes and file paths remain untranslated.
- Portal title/subtitle and unit overrides should support future English/Hindi content.
- Default language is represented as `en-IN` or `hi-IN`.
