# API USAGE

## Current State
- UI uses sample data only.
- No setup API calls are made.
- No files are uploaded.

## Future Integration
- Persist portal identity, language, public dashboard, unit branding, and theme token settings through a governed API.
- Upload assets only through approved storage API and never by directly writing arbitrary local paths from the browser.
