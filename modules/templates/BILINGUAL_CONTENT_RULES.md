# BILINGUAL CONTENT RULES

- Default locale is `en-IN`.
- Use API labels for template names, axes, members, measures, and validation rule labels.
- Allow Hindi text expansion in grid headers and side panels.
