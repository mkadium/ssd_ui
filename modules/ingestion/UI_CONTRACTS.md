# UI CONTRACTS

## Ingestion UI Contract
- Read-only submission, version, event, manifest, job, run, and staged-record views are allowed.
- DEV-local JSON submit may be planned against the approved API contract.
- Do not show raw payload bodies, source hashes, token hashes, or internal IDs.
