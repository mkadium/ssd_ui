# RESPONSIVE TEST NOTES

- Monitor table uses existing responsive table patterns.
- Detail modal uses responsive grids that collapse to single column on smaller screens.
- Long codes and contact fields wrap where needed.
