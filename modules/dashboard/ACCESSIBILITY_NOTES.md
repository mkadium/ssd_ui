# ACCESSIBILITY NOTES

Dashboard charts must include textual summaries, accessible labels, keyboard-reachable drilldowns, and table alternatives for critical data.

Current implementation starts this baseline with semantic headings, skip link support through the App Shell, visible focus styles, button-based chart/card interactions, table summaries, and text labels beside chart content. Full keyboard and screen reader evidence still needs manual/browser verification.
